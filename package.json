{
  "name": "onboarding",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dependencies": "npm install && cd client && npm install && cd .. && cd server && npm install && cd ..",
    "server": "node server/app.js",
    "client": "cd client && npm start && cd ..",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^3.5.1"
  },
  "dependencies": {}
}
